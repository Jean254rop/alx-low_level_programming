#include "main.h"
/**
 * _isdigit - program for task one to check digits
 * @c:to be checked
 * Return: 1 for uppercase 0 for anything
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
return (0);
}
