#include <stdio.h>
/**
 * main - a program that prints its name and a new line
 * @argc: number of arguments
 * @argv: arguments array
 * Return: 0
 */
int main(int *argv[])
{
printf("%s\n", *argv);
return (0);
}
